---
import { Icon } from 'astro-icon/components';
import Reveal from '@/components/widgets/Reveal.astro';

const reviews = [
  {
    author: 'Richard',
    role: 'Community Organiser',
    company: 'CloudScale',
    quote: 'This platform completely changed how our support team operates. From intelligent workflows to real-time analytics, everything is designed to scale. We now handle more than double the number of tickets with the same team without compromising service quality.',
  },
  {
    author: 'Rudra Ghosh',
    role: 'Product Lead',
    company: 'Fintech Flow',
    quote: 'Connecting custom LLM endpoints and databases took literally minutes. The reliability and low latency execution have made Zylo an indispensable part of our core product infrastructure.',
  },
  {
    author: 'Sarah Jenkins',
    role: 'VP of Operations',
    company: 'HyperLogistics',
    quote: 'We saved over 150 hours per team member per month. The visual workflow canvas makes it effortless for anyone on our team to build automated AI pipelines.',
  },
];
---

<section id="testimonials" class="py-24 bg-canvas border-b border-hairline-soft overflow-hidden">
  <div class="container-page max-w-6xl mx-auto px-4">
    <Reveal class="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-16">
      <div>
        <span class="text-sm font-semibold text-stone uppercase tracking-wider mb-2 block">Testimonials</span>
        <h2 class="text-3xl sm:text-5xl font-semibold text-ink font-heading tracking-tight leading-tight">
          Experiences shared by <br class="hidden sm:inline" /> our customers
        </h2>
      </div>
      <a href="/testimonial" class="inline-flex h-11 items-center justify-center gap-2 rounded-full bg-canvas-dark px-6 text-sm font-semibold text-on-dark hover:bg-charcoal transition-colors shadow-[var(--shadow-subtle)] shrink-0">
        See all testimonials <Icon name="lucide:arrow-right" class="w-4 h-4" />
      </a>
    </Reveal>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {reviews.map((r, idx) => (
        <Reveal delay={idx * 80}>
          <div class="rounded-lg border border-hairline bg-surface-soft p-8 hover:bg-canvas hover:shadow-[var(--shadow-card)] transition-all h-full flex flex-col justify-between">
            <div class="mb-8">
              <div class="flex items-center gap-1 text-brand-warn mb-6">
                <Icon name="lucide:star" class="w-4 h-4 fill-current" />
                <Icon name="lucide:star" class="w-4 h-4 fill-current" />
                <Icon name="lucide:star" class="w-4 h-4 fill-current" />
                <Icon name="lucide:star" class="w-4 h-4 fill-current" />
                <Icon name="lucide:star" class="w-4 h-4 fill-current" />
              </div>
              <p class="text-steel text-sm leading-relaxed font-normal italic">
                "{r.quote}"
              </p>
            </div>

            <div class="pt-6 border-t border-hairline flex items-center gap-4">
              <div class="w-11 h-11 rounded-full bg-primary text-on-primary font-semibold flex items-center justify-center font-heading text-sm shadow-[var(--shadow-subtle)]">
                {r.author[0]}
              </div>
              <div>
                <h4 class="font-semibold text-ink text-sm font-heading">{r.author}</h4>
                <p class="text-xs text-stone">{r.role}, <span class="font-semibold text-ink">{r.company}</span></p>
              </div>
            </div>
          </div>
        </Reveal>
      ))}
    </div>
  </div>
</section>
