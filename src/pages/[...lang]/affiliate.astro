---
import Layout from '@/layouts/Base.astro';
import Navbar from '@/components/global/header/Navbar.astro';
import Footer from '@/components/global/Footer.astro';
import Reveal from '@/components/widgets/Reveal.astro';
import { generatePaths } from '@/lib/utils/i18nUtils';
import { Icon } from 'astro-icon/components';

export function getStaticPaths() {
  return generatePaths();
}

const tiers = [
  {
    name: 'Starter affiliates',
    subtitle: 'New affiliate benefits:',
    items: ['Up to $25 per signup', 'Resource guide', 'Exclusive affiliate assets'],
  },
  {
    name: 'Advanced partners',
    subtitle: 'Send 100 signups in a month:',
    items: ['10% discount offer', 'Bonus Opportunities'],
  },
  {
    name: 'Premier partners',
    subtitle: 'Send 200 new signups in a month:',
    items: ['Custom reporting and creative', 'Dedicated landing pages', '1x1 account support'],
  },
];

const faqs = [
  {
    q: 'How does an AI resume builder work?',
    a: 'When you enter your experience and the specific job you’re targeting, the AI analyzes the job description and tailors your resume to match the key skills, keywords.',
  },
  {
    q: 'Can the AI customize resumes for different jobs?',
    a: 'When you enter your experience and the specific job you’re targeting, the AI analyzes the job description and tailors your resume to match the key skills, keywords.',
  },
  {
    q: 'Does it improve my chances of getting hired?',
    a: 'When you enter your experience and the specific job you’re targeting, the AI analyzes the job description and tailors your resume to match the key skills, keywords.',
  },
  {
    q: 'Does it have templates for different industries?',
    a: 'When you enter your experience and the specific job you’re targeting, the AI analyzes the job description and tailors your resume to match the key skills, keywords.',
  },
  {
    q: 'What file formats can I download (PDF, TXT, DOCX)?',
    a: 'When you enter your experience and the specific job you’re targeting, the AI analyzes the job description and tailors your resume to match the key skills, keywords.',
  },
];
---

<Layout title="Affiliate Program — Let’s join our Zylo’s Affiliate program" description="Become an affiliate and get rewarded for sharing the world's fastest-growing productivity platform with your network.">
  <Navbar />

  <main id="main" class="pt-24 md:pt-[120px] pb-24 bg-surface-soft">
    <div class="container-page max-w-6xl mx-auto px-4">
      {/* 1. Affiliate hero */}
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-10 items-center mb-24">
        <Reveal class="lg:col-span-7">
          <h1 class="text-4xl sm:text-5xl font-semibold text-ink font-heading tracking-tight mb-6 leading-tight">
            Let’s join our Zylo’s Affiliate program
          </h1>
          <p class="text-lg text-steel leading-relaxed mb-10">
            Become an affiliate and get rewarded for sharing the world's fastest-growing productivity platform with your network.
          </p>
          <a href="/register" class="inline-flex h-12 items-center justify-center gap-2 rounded-full bg-primary px-8 text-sm font-semibold text-on-primary hover:bg-charcoal transition-colors shadow-[var(--shadow-subtle)]">
            Join now <Icon name="lucide:arrow-right" class="w-4 h-4" />
          </a>
        </Reveal>

        <Reveal delay={100} class="lg:col-span-5">
          <div class="rounded-lg border border-hairline bg-canvas p-8 shadow-[var(--shadow-subtle)]">
            <div class="w-12 h-12 rounded-md bg-brand/10 text-brand flex items-center justify-center mb-6">
              <Icon name="lucide:dollar-sign" class="w-6 h-6" />
            </div>
            <p class="text-sm font-semibold text-ink font-heading mb-2">Up to $25 per signup</p>
            <p class="text-xs text-steel leading-relaxed">
              Share your link, track every referral and get paid for the workspaces you bring to Zylo.
            </p>
          </div>
        </Reveal>
      </div>

      {/* 2. Payment details */}
      <Reveal class="text-center max-w-3xl mx-auto mb-24">
        <span class="inline-block border border-hairline bg-canvas px-4 py-1.5 rounded-full text-xs font-semibold text-ink uppercase tracking-wider mb-6 shadow-[var(--shadow-subtle)]">
          Payment details
        </span>
        <h2 class="text-3xl sm:text-5xl font-semibold text-ink font-heading tracking-tight mb-4 leading-tight">
          Earn by helping others boost productivity
        </h2>
        <p class="text-lg text-steel leading-relaxed">
          Get up to $25 for every new free workspace referral — no confusing conditions, just results.
        </p>
      </Reveal>

      {/* 3. Tiered Structure */}
      <Reveal class="text-center mb-10">
        <h2 class="text-3xl sm:text-5xl font-semibold text-ink font-heading tracking-tight">
          Tiered Structure
        </h2>
      </Reveal>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-24 items-stretch">
        {tiers.map((t, idx) => (
          <Reveal delay={idx * 80}>
            <div class="rounded-lg border border-hairline bg-canvas p-8 shadow-[var(--shadow-subtle)] hover:border-brand/40 hover:shadow-[var(--shadow-subtle)] transition-all h-full">
              <div class="border-b border-hairline-soft pb-5 mb-7">
                <h3 class="text-xl font-semibold text-ink font-heading mb-2">{t.name}</h3>
                <p class="text-xs text-stone font-medium">{t.subtitle}</p>
              </div>
              <ul class="space-y-3">
                {t.items.map((i) => (
                  <li class="flex items-center gap-2.5 text-xs text-steel font-medium">
                    <Icon name="lucide:check" class="w-4 h-4 text-brand shrink-0" />
                    <span>{i}</span>
                  </li>
                ))}
              </ul>
            </div>
          </Reveal>
        ))}
      </div>

      {/* 4. FAQ */}
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
        <Reveal class="lg:col-span-5">
          <h2 class="text-3xl sm:text-4xl font-semibold text-ink font-heading tracking-tight mb-3">
            Need help?
          </h2>
          <p class="text-steel leading-relaxed">
            Get quick answers and friendly assistance anytime you need.
          </p>
        </Reveal>

        <div class="lg:col-span-7 space-y-3">
          {faqs.map((f, idx) => (
            <Reveal delay={idx * 60}>
              <details class="group rounded-lg border border-hairline bg-canvas px-7 py-5 shadow-[var(--shadow-subtle)]">
                <summary class="flex cursor-pointer items-center justify-between gap-4 list-none">
                  <span class="text-base font-semibold text-ink font-heading">{f.q}</span>
                  <span class="shrink-0 w-8 h-8 rounded-full border border-hairline flex items-center justify-center text-stone group-open:bg-canvas-dark group-open:text-on-dark transition-colors">
                    <Icon name="lucide:plus" class="w-4 h-4 group-open:hidden" />
                    <Icon name="lucide:minus" class="w-4 h-4 hidden group-open:block" />
                  </span>
                </summary>
                <p class="pt-4 text-sm text-steel leading-relaxed">{f.a}</p>
              </details>
            </Reveal>
          ))}
        </div>
      </div>
    </div>
  </main>

  <Footer />
</Layout>
