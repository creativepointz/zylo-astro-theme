---
import Layout from '@/layouts/Base.astro';
import Navbar from '@/components/global/header/Navbar.astro';
import Footer from '@/components/global/Footer.astro';
import Reveal from '@/components/widgets/Reveal.astro';
import { generatePaths } from '@/lib/utils/i18nUtils';
import { Icon } from 'astro-icon/components';

export function getStaticPaths() {
  return generatePaths();
}

const offices = [
  { country: 'Colombia', address: 'Bogota D.C., Colombia, b. a 181 C No. 930 Ap 202', main: false },
  { country: 'France', address: '9 Pearse Street, Kinsale, Cork, P17 AH66, Ireland', main: true },
  { country: 'Egypt', address: "Av. Cordoba 1309, 3'A, City of Buenos Aires, Egypt", main: false },
];

const demoDetails = [
  { icon: 'lucide:mail', value: 'contact@zylo.co', href: 'mailto:contact@zylo.co' },
  { icon: 'lucide:phone', value: '+1-202-555-0144', href: 'tel:+12025550144' },
  { icon: 'lucide:map-pin', value: '35Park Avenue, Uk', href: '#' },
];
---

<Layout title="Contact us — Zylo" description="Our support team is here to assist you reach out anytime.">
  <Navbar />

  <main id="main" class="pt-24 md:pt-[120px] pb-24 bg-surface-soft">
    <div class="container-page max-w-6xl mx-auto px-4">
      {/* 1. Contact hero */}
      <Reveal class="text-center max-w-3xl mx-auto mb-16">
        <span class="inline-block border border-hairline bg-canvas px-4 py-1.5 rounded-full text-xs font-semibold text-ink uppercase tracking-wider mb-6 shadow-[var(--shadow-subtle)]">
          Contact us
        </span>
        <h1 class="text-4xl sm:text-6xl font-semibold text-ink font-heading tracking-tight">
          Our support team is here to assist you reach out anytime.
        </h1>
      </Reveal>

      {/* 2. Offices */}
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-20">
        {offices.map((o, idx) => (
          <Reveal delay={idx * 80}>
            <div class="rounded-lg border border-hairline bg-canvas p-8 shadow-[var(--shadow-subtle)] h-full">
              <div class="flex items-center justify-between gap-3 mb-4">
                <div class="w-12 h-12 rounded-md bg-brand/10 text-brand flex items-center justify-center">
                  <Icon name="lucide:map-pin" class="w-6 h-6" />
                </div>
                {o.main && (
                  <span class="rounded-full border border-hairline bg-surface-soft px-3 py-1 text-[11px] font-semibold text-steel">
                    Main Office
                  </span>
                )}
              </div>
              <h2 class="text-xl font-semibold text-ink font-heading mb-2">{o.country}</h2>
              <p class="text-steel text-sm leading-relaxed">{o.address}</p>
            </div>
          </Reveal>
        ))}
      </div>

      {/* 3. Let's contact us form */}
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-10 items-start mb-20">
        <Reveal>
          <span class="inline-block border border-hairline bg-canvas px-4 py-1.5 rounded-full text-xs font-semibold text-ink uppercase tracking-wider mb-6 shadow-[var(--shadow-subtle)]">
            Let’s contact us
          </span>
          <h2 class="text-3xl sm:text-5xl font-semibold text-ink font-heading tracking-tight mb-4">
            Get in touch with our support for help.
          </h2>
          <p class="text-lg text-steel leading-relaxed">
            We accept smoke signals, but modern communication works better today.
          </p>
        </Reveal>

        <Reveal delay={100} class="rounded-lg border border-hairline bg-canvas p-8 md:p-10 shadow-[var(--shadow-card)]">
          <form class="space-y-4">
            <input
              type="text"
              placeholder="Your full name"
              class="w-full rounded-md border border-hairline px-5 py-3 text-sm text-ink placeholder:text-stone focus:border-brand focus:outline-none"
            />
            <input
              type="text"
              placeholder="Your email address"
              class="w-full rounded-md border border-hairline px-5 py-3 text-sm text-ink placeholder:text-stone focus:border-brand focus:outline-none"
            />
            <textarea
              rows="4"
              placeholder="Write your message"
              class="w-full resize-none rounded-md border border-hairline px-5 py-3 text-sm text-ink placeholder:text-stone focus:border-brand focus:outline-none"
            ></textarea>
            <label class="flex items-center gap-2 text-sm text-steel">
              <input type="checkbox" class="h-4 w-4 rounded border-hairline text-brand focus:ring-brand" />
              I agree to receive marketing materials
            </label>
            <button
              type="submit"
              class="w-full rounded-full bg-primary py-3.5 text-sm font-semibold text-on-primary hover:bg-charcoal transition-colors shadow-[var(--shadow-subtle)]"
            >
              Send Analysis
            </button>
          </form>
        </Reveal>
      </div>

      {/* 4. Book a Demo */}
      <Reveal class="rounded-lg border border-hairline bg-canvas p-8 md:p-12 shadow-[var(--shadow-subtle)] text-center">
        <h2 class="text-3xl sm:text-5xl font-semibold text-ink font-heading tracking-tight mb-2">Book a Demo!</h2>
        <p class="text-lg text-steel leading-relaxed mb-10">
          We will contact again after receive your request in 24h
        </p>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          {demoDetails.map((d) => (
            <a
              href={d.href}
              class="group rounded-lg border border-hairline bg-surface-soft/60 p-6 hover:border-brand/40 hover:bg-canvas transition-all flex flex-col items-center gap-3"
            >
              <span class="w-10 h-10 rounded-md bg-brand/10 text-brand flex items-center justify-center">
                <Icon name={d.icon} class="w-5 h-5" />
              </span>
              <span class="text-sm font-semibold text-ink group-hover:text-brand transition-colors">{d.value}</span>
            </a>
          ))}
        </div>
      </Reveal>
    </div>
  </main>

  <Footer />
</Layout>
