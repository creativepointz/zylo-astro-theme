---
import Layout from '@/layouts/Base.astro';
import Navbar from '@/components/global/header/Navbar.astro';
import Footer from '@/components/global/Footer.astro';
import Reveal from '@/components/widgets/Reveal.astro';
import { generatePaths } from '@/lib/utils/i18nUtils';
import { Icon } from 'astro-icon/components';

export function getStaticPaths() {
  return generatePaths();
}
---

<Layout title="Login — Welcome To Your Zylo System" description="Sign in with email or continue with Google to access your Zylo system.">
  <Navbar />

  <main id="main" class="pt-24 md:pt-[120px] pb-24 bg-surface-soft">
    <div class="container-page max-w-6xl mx-auto px-4">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-10 items-stretch">
        {/* 1. Welcome panel */}
        <Reveal class="lg:col-span-5">
          <div class="h-full rounded-lg bg-canvas-dark p-10 flex flex-col justify-between">
            <h1 class="text-3xl sm:text-4xl font-semibold text-on-dark font-heading tracking-tight leading-tight">
              Welcome To Your Zylo System.
            </h1>
            <div class="mt-12 grid grid-cols-2 gap-4">
              <div class="rounded-lg border border-white/10 bg-canvas/5 p-5">
                <Icon name="lucide:layout-dashboard" class="w-6 h-6 text-on-dark mb-3" />
                <p class="text-xs font-medium text-on-dark-muted leading-relaxed">Real-time collaboration</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-canvas/5 p-5">
                <Icon name="lucide:shield-check" class="w-6 h-6 text-on-dark mb-3" />
                <p class="text-xs font-medium text-on-dark-muted leading-relaxed">Two factor authentication (2FA)</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-canvas/5 p-5">
                <Icon name="lucide:sparkles" class="w-6 h-6 text-on-dark mb-3" />
                <p class="text-xs font-medium text-on-dark-muted leading-relaxed">AI-Powered task assistant</p>
              </div>
              <div class="rounded-lg border border-white/10 bg-canvas/5 p-5">
                <Icon name="lucide:bar-chart-3" class="w-6 h-6 text-on-dark mb-3" />
                <p class="text-xs font-medium text-on-dark-muted leading-relaxed">Time tracking &amp; productivity reports</p>
              </div>
            </div>
          </div>
        </Reveal>

        {/* 2. Sign in form */}
        <Reveal delay={100} class="lg:col-span-7">
          <div class="h-full rounded-lg border border-hairline bg-canvas p-8 md:p-12 shadow-[var(--shadow-subtle)]">
            <h2 class="text-2xl font-semibold text-ink font-heading mb-8">Register Account</h2>

            <div class="flex flex-wrap gap-3 mb-8">
              <a href="#" class="inline-flex h-14 items-center gap-2.5 rounded-md border border-hairline px-8 text-sm font-medium text-ink hover:border-ink transition-colors">
                <Icon name="lucide:globe" class="w-5 h-5" />
                Continue with Google
              </a>
              <a href="#" class="inline-flex h-14 items-center justify-center rounded-md border border-hairline px-8 text-ink hover:border-ink transition-colors">
                <Icon name="lucide:command" class="w-5 h-5" />
              </a>
            </div>

            <div class="flex items-center gap-4 mb-8">
              <span class="h-px flex-1 bg-hairline"></span>
              <span class="text-sm text-stone">Or Sign In with email</span>
              <span class="h-px flex-1 bg-hairline"></span>
            </div>

            <form class="space-y-5">
              <div>
                <label class="block text-xs font-semibold text-steel mb-1.5">Your Email</label>
                <input type="email" required class="w-full h-14 rounded-md border border-hairline px-5 text-sm text-ink focus:border-brand focus:outline-none" />
              </div>
              <div>
                <label class="block text-xs font-semibold text-steel mb-1.5">Password</label>
                <input type="password" required class="w-full h-14 rounded-md border border-hairline px-5 text-sm text-ink focus:border-brand focus:outline-none" />
              </div>

              <div class="flex items-center justify-between gap-4">
                <label class="flex items-center gap-2 text-sm text-stone cursor-pointer">
                  <input type="checkbox" class="rounded border-hairline text-brand focus:ring-brand" />
                  Remember me
                </label>
                <a href="#" class="text-sm text-ink hover:text-brand transition-colors">Forgot password ?</a>
              </div>
            </form>

            <div class="mt-8">
              <button type="submit" class="inline-flex h-14 items-center justify-center rounded-md bg-primary px-20 text-sm font-semibold text-on-primary hover:bg-charcoal transition-colors shadow-[var(--shadow-subtle)]">
                Register
              </button>
              <p class="mt-8 text-sm text-stone">
                Don't have account ? <a href="/register" class="font-medium text-brand hover:underline">Register</a>
              </p>
            </div>
          </div>
        </Reveal>
      </div>
    </div>
  </main>

  <Footer />
</Layout>
